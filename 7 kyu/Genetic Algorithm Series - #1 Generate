 public string Generate(int length)
        {
            string chromosome = "";
            Random rnd = new Random();
            for (int i = 0; i < length; i++)
            {
                chromosome += rnd.Next(2);
            }
            return chromosome;
        }
