 public string Generate(int length)
        {
            string output = "";
            Random rnd = new Random();
            for (int i = 0; i < length; i++)
            {
                output += rnd.Next(2);
            }
            return output;
        }
