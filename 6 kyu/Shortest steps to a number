public static int ShortestStepsToNum(int num)
{
  int steps = 0;

  while (num > 1)
  {
      steps++;

      if (num % 2 == 0)
      {
          num /= 2;
      }
      else
      {
          num--;
      }
  }

  return steps;
}
